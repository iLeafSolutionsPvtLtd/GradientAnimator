import XCTest
@testable import GradientAnimator

final class GradientAnimatorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GradientAnimator().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
